// config.js

// let apiBaseURL;

// if (process.env.NODE_ENV === 'production') {
//   // Environnement de production (Vercel, Netlify, etc.)
//   apiBaseURL = process.env.REACT_APP_PROD_API_BASE_URL;
// } else {
//   // Environnement local
//   apiBaseURL =
//     process.env.REACT_APP_LOCAL_API_BASE_URL || 'https://ekommerce-back.vercel.app/api/';
// }
// 

    const apiBaseURL = 'https://ekommerce-back.vercel.app/api';
export default {
  apiBaseURL,
};
